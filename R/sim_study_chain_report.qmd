---
title: "chain_report"
format: pdf
editor: visual
editor_options: 
  chunk_output_type: console
params:
  model_data_file: runs/sim_study_hanam_2018_2/model_data.hdf5
  chain_file: runs/sim_study_hanam_2018_2/chain.parquet
---

## Simulation study MCMC fit

```{r}
#| echo: false

library(targets)
suppressMessages(tar_source())

```

{{< pagebreak >}}

```{r}
#| echo: false

true_values <- tribble(
  ~name, ~value,
  "mu_long", 2.0,
  "mu_sum", 2.0 + 2.0,
  "mu_short", 2.0,
  "sigma_long", 0.2,
  "sigma_short", 0.1,
  "tau", 0.05
)

model_data <- read_model_data(params$model_data_file)

chain <- read_chain(params$chain_file)

chain <- chain %>%
  mutate(mu_short = mu_sum - mu_long)

warmup_steps <- min(3500, max(chain$.iteration) - 1)

parnames <- c(colnames(chain)[5:9], "mu_short")

chain %>%
  select(.iteration, .chain, any_of(parnames)) %>%
  pivot_longer(any_of(parnames)) %>%
  ggplot() +
  geom_line(aes(x = .iteration, y = value, colour = factor(.chain))) +
  
  geom_vline(xintercept = warmup_steps) +
  
  facet_wrap(~name, scales = "free_y")

chain_filt <- chain %>%
  filter(.iteration > warmup_steps)

chain_filt_wide <- chain_filt %>% 
  select(.iteration, .chain, any_of(parnames)) %>%
  pivot_longer(any_of(parnames))

ggplot() +
  geom_line(aes(x = .iteration, y = value, colour = factor(.chain)),
            chain_filt_wide) +
  facet_wrap(~name, scales = "free_y")

ggplot() +
  geom_histogram(aes(x = value), bins = 50,
                 chain_filt_wide) +
  
  geom_vline(aes(xintercept = value), true_values) +
  
  facet_wrap(~name, scales = "free")


ggplot() +
  geom_tile(aes(x = ix_t, y = ix_subject, fill = p),
            get_inf_data(chain_filt, model_data)) +
  
  geom_point(aes(x = ix_t, y = ix_subject), model_data$infections, colour = "red")


plot_data_inf <- get_inf_data(chain_filt, model_data)
autoplot(precrec::evalmod(scores = plot_data_inf$p, labels = plot_data_inf$inf), curvetype = "ROC")


ggplot() +
  geom_line(aes(x = .iteration, y = accuracy),
            get_inf_accuracy(chain, model_data))
```
